# Backend API base URL (the Wikala backend service)
NEXT_PUBLIC_API_URL=https://wikala-back.nodeteam.site/api/v1

# Google Maps JS API key — optional; needed for location pickers/maps
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
